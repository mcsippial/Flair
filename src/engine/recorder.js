// Phase 2: Audio recording via getUserMedia
// Intended implementation:
// - getUserMedia({ audio: true }) on arm
// - MediaRecorder API to capture audio stream
// - Convert recorded blob to AudioBuffer via Tone.js
// - Create audio clip in session state on stop
// - Waveform preview via OfflineAudioContext analysis
export function initRecorder() {
  console.warn('Recorder not implemented — Phase 2 feature');
}
